#!/bin/sh

if [ "$1" ]; then
	cd ~/"$1"-* 2>/dev/null || {
		echo "Level '$1' does not exist";
		exit 1;
	}
fi

while [ "$PWD" != / ] && ! [ -f README ]; do cd ..; done
less -R README 2>/dev/null || echo '❌ No tasks available, are you in the correct directory?' >&2
