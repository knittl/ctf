#!/bin/sh
if [ "$#" -ne 4 ]; then
	echo "$0: Usage: $0 SCRIPT TASK MAC FILE..." >&2
	exit 1
fi

script="$1"; task="$2"; mac="$3";
shift 3
nonce="$("$script" "$@")" || exit 1
printf '%s{%s:%s:%s:%s}\n' "$COURSE" "$task" "$STUDENT" "$nonce" "$mac"

